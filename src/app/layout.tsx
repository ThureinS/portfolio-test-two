import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/theme-provider";

const inter = Inter({ 
  subsets: ["latin"],
  variable: "--font-inter",
});

const playfair = Playfair_Display({ 
  subsets: ["latin"],
  variable: "--font-playfair",
});

export const metadata: Metadata = {
  title: "Thureinsoe | Web Developer Portfolio",
  description: "Highly creative, continuously learning, and resilient tech enthusiast with a strong passion for technology. Frontend web developer specializing in modern web technologies.",
  keywords: ["Web Developer", "Frontend Developer", "Next.js", "React", "TypeScript", "Portfolio"],
  authors: [{ name: "Thureinsoe" }],
  openGraph: {
    title: "Thureinsoe | Web Developer Portfolio",
    description: "Frontend web developer specializing in modern web technologies",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning className="scroll-smooth">
      <body className={`${inter.variable} ${playfair.variable} font-sans antialiased`}>
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange={false}
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
